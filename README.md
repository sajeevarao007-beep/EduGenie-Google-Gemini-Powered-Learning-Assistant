# 🚀 EduGenie Learning Assistant – Local Deployment

---

## 🎯 Objective

This phase focused on deploying and running the **EduGenie Learning Assistant** in a local development environment using:

- FastAPI  
- Uvicorn  

The goal was to ensure that the complete frontend–backend system works correctly and can be accessed through a web browser for real-time interaction.

---

## ⚙️ Running the Application

The application is executed locally using the following command:

```bash
uvicorn main:app --reload
